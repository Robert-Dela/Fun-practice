const getUserList = require('../components/users');

describe('', () => {
    test('should ', () => {
        const userList = await getUserList();
        expect(userList[0].id).toBe(1);
    })
    
})